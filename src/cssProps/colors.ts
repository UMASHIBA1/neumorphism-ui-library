import { css } from "styled-components";
import { bgColor as backgroundColor, gray } from "../constants/colors";

export const bgColor = css`
  background-color: ${backgroundColor};
`;

export const grayText = css`
  color: ${gray};
`;
